# Q010 Closure — Aggregate M10 Closure and M11 Readiness

Status: blocked; closure attempted 2026-09-09

Plan: [Q010 — aggregate M10 closure and M11 readiness report](../../implementation/qualification/010-aggregate-m10-closure-and-m11-readiness.md)

Current main implementation SHA: `7a385824c5a79c3141f899b5f0b8c9ae21bd1711`

Q010 is formally closed as a blocked closure attempt. The deterministic and
environment evidence was aggregated on current `main`, but M10 cannot close
because the mandatory Q007 live-provider cells were not accepted. This record
does not authorize M11 cutover or rewrite any predecessor closure.

## Q001 manifest and aggregate result

The frozen manifest is `m10-q001.v1` with 101 mandatory cells: 87 exact, 4
semantic, 5 characterization, and 5 manual-review cells. It has 93 reuse
assignments and 8 owner-only assignments. No cell is unowned. The current
aggregate disposition is:

| Owner | Current result | Q010 disposition |
|---|---|---|
| Q002 | 18/18 deterministic cells pass | accepted evidence; current rerun pass |
| Q003 | 4/4 aggregate commands pass, covering its 5 assigned cells | accepted evidence; current rerun pass |
| Q004 | 14/14 pages, 54 static/theme assets, 51 themes pass | accepted evidence; current rerun pass |
| Q005 | accepted target matrix; current Linux aarch64 portability rerun pass | accepted evidence; freshness recorded below |
| Q006 | accepted disposable rootful Linux run | accepted evidence; no deployment-source change after acceptance |
| Q007 | live cell blocked after provider-edge HTTP 403 | blocking finding |
| Q008 | physical Raspberry Pi 5 run pass, dependency blocked | not promoted because Q007 is not accepted |
| Q009 | six-phase deterministic run pass, dependency blocked | not promoted because Q008 is not accepted |

The Q002, Q003, and Q004 machine-readable artifacts were refreshed by the
current-main rerun. Their SHA-256 values are:

| Artifact | SHA-256 |
|---|---|
| [`002-run.json`](002-run.json) | `78c0523fe491e94f6176a58de8f9d0f2a67f1a7d9b635eac286a9f98f616a873` |
| [`002-run.md`](002-run.md) | `3c39a7f4455ca82b359f60a1c4da23b6230c4ace5e89d395848404091c62764d` |
| [`003-run.json`](003-run.json) | `69070bc14812510115b59d86ff7485c8d8fb8927062b2fb118e50af033032573` |
| [`003-run.md`](003-run.md) | `14902580a7bc902a3af4b4a01842fe459e16311421465ec9340951381ea0f47d` |
| [`004-run.json`](004-run.json) | `e5765937585e3c4d104a1313247ad077ea2c0c9430f87635e6881914f45bdb9a` |

The current Linux aarch64 Q005 rerun was intentionally kept in a private
temporary artifact rather than added to the repository. It passed the
manifest, dependency, startup, health, readiness, finite/streaming,
rehash, backup/recover, restart, and cleanup observations in 4.864 seconds.
The current Q007 offline loopback rerun also passed all seven bounded cells in
3.760 seconds. Neither is live-provider evidence.

## Mandatory current-main verification

The following commands were run from the current main SHA above:

```text
cargo fmt --manifest-path rust/Cargo.toml --all -- --check                 # pass
cargo clippy --manifest-path rust/Cargo.toml --all-targets -- -D warnings # pass
cargo test --manifest-path rust/Cargo.toml --all-targets -- --test-threads=1 # pass; 440 tests
uv run python scripts/qualification_runner.py --skip-build                 # 18 pass, 0 fail/block/skip
uv run python scripts/qualification_database.py --skip-build               # 4 pass, 0 fail/infrastructure-error
uv run python scripts/qualification_dashboard.py --screenshots             # pass
uv run pytest tests/migration_rs -q --tb=short --maxfail=1                  # 157 passed, 4 skipped
uv run pytest tests/smoke/ -q --tb=short --maxfail=1                        # 14 passed
uv run pyright src/ scripts/                                            # 0 errors, 0 warnings, 0 informations
uv run ruff format --check src/ tests/ scripts/                            # 751 files formatted
uv run ruff check src/ tests/ scripts/                                     # pass
git diff --check                                                           # pass
```

Q004's screenshot/manual-review metadata was regenerated by its current-main
qualification command. No dashboard renderer or asset source changed after
the accepted visual review, and no material visual difference was found.

## Evidence summary

### Data and dashboard

The Q003 headline Python-to-Rust-to-backup/restore-to-Python transition and
its faulted restore/recovery cases remain accepted; the current Q003 aggregate
passed all four commands. Schema/checksum compatibility, ordinary Rust writes,
backup archive validation, isolated recovery, and final Python readability
remain green. Q004's 14-page DOM/static/theme comparison and visual review
remain accepted, with no renderer or asset drift in the current rerun.

### Targets, rootful Linux, and freshness

The accepted Q005 matrix qualifies Linux x86_64 and Linux aarch64 as supported
and macOS arm64 as supported-development; Windows remains unsupported and
other Unix remains not qualified. The current host is Linux aarch64, and the
current portability rerun passed its applicable target. The accepted Q006
disposable Ubuntu/systemd evidence remains the rootful Linux record.

The freshness audit found that the Q007 implementation changed provider/wire
and configured-preference behavior, which was exercised by the Q007 live
attempt and current Q008/Q009 Linux runs. No deployment implementation changed
after Q006, so Q006 was not unnecessarily repeated on a non-disposable host.
The accepted hosted Q005 artifacts remain the evidence for Linux x86_64 and
macOS arm64; no cross-platform claim is widened by this record.

### Live provider

Q007 resolved the three planned OpenCode Go model identifiers, but its first
real Responses request received HTTP 403 from the provider edge. The run
stopped within the seven-request budget, so the remaining finite, streaming,
and cross-surface live cells have no evidence. The offline loopback matrix
passed 7/7 and proves harness safety only. Credentials were read from the
external `OPENCODE_GO_KEY_1` variable and no credential or raw body was
persisted.

### ARM64 SBC and stability

The accepted physical Q008 evidence is a Raspberry Pi 5 Model B on Ubuntu
24.04.4/Linux 6.8/aarch64. All functional cells passed; the five samples held
at 14 file descriptors and 2 threads, RSS ranged from 15.8 MiB to 18.6 MiB,
and bounded work converged to 8 completed requests, 8 terminal attempts, 8
completed reservations, zero pending requests, and zero active reservations.
These are characterization values, not SLAs.

Q009's current deterministic run passed warmup, mixed traffic, bounded faults,
reload/background churn, abrupt restart/recovery, and final convergence. It
observed 30 requests, 36 terminal attempts/reservations, zero pending requests,
zero active reservations, zero active leases, zero retiring generations, and
zero finalization jobs at convergence. Its first complete Rust run had one
timing-sensitive existing-test flake; the exact test and immediate rerun then
passed, followed by the complete serial suite. No Q009 defect remains.

## Findings ledger

| ID | Severity | Affected cells/target/provider | Symptom and invariant | Owner | Fixing plan | Before/after regression | Status |
|---|---|---|---|---|---|---|---|
| Q010-F001 | medium | Q001 live provider cells; OpenCode Go Responses and dependent stream/cross-surface cells | Provider edge returned HTTP 403 before a successful real interaction; mandatory live evidence is absent | provider transport/wire qualification | [Q011](../../implementation/qualification/011-q007-live-provider-corrective-closure.md) | Before: no real success evidence. After: deterministic 7/7 loopback and harness redaction checks pass; live remains unresolved | open/blocking |
| Q010-F002 | informational | Q008/Q009/Q010 dependency chain | Physical SBC and sustained local evidence pass, but predecessor acceptance rules prohibit promotion while Q007 is blocked | qualification planning | Q011 followed by dependency-order re-acceptance | Current Q008/Q009 artifacts remain append-only and no state is falsely promoted | open/planning blocker |

No high-severity finding or additional correctness, security, data-loss,
dashboard, lifecycle, resource, or portability defect was found in the
current deterministic review. Q010-F001 is sufficient to prevent M10 closure.

## Compatibility delta report

| Surface | Qualified result | Intentional difference/limit |
|---|---|---|
| Config, CLI, API, routing, retry, lifecycle | deterministic parity green | approved dynamic IDs/paths/timestamps only |
| OpenAI Chat, OpenAI Responses, Anthropic Messages and provider wire codecs | deterministic/cross-surface parity green | live provider acceptance remains open under Q010-F001 |
| SQLite migrations, usage, backup/recover | bidirectional Q003 transition accepted | no schema fork or rollback waiver |
| Dashboard SSR/DOM/assets/themes | Q004 accepted | no redesign or renderer drift |
| Linux x86_64, Linux aarch64, macOS arm64 | Q005 accepted matrix | macOS is supported-development; other Unix not qualified; Windows unsupported |
| Rootful Linux/systemd/cron/logrotate | Q006 accepted on disposable Linux | not a universal non-Linux deployment claim |
| ARM64 SBC | Q008 physical evidence pass | one-board characterization, not an SLA or fleet guarantee |
| Sustained stability/resource ownership | Q009 deterministic convergence pass | bounded workload, not throughput benchmarking |

No external behavior difference was newly authorized by Q010. Python remains
the production/install/update authority. Rust remains a side-by-side
candidate; config, DB, runtime, backup, and dashboard paths remain preserved.

## M11 readiness checklist

M11 is **not eligible** for its separate planning review because accepted Q010
closure is missing. The following remain M11 prerequisites:

| Prerequisite | Q010 result | M11 ownership |
|---|---|---|
| Supported release target matrix | evidence exists, final release matrix not authorized | freeze artifact names/targets and release policy |
| Binary naming/build strategy | build evidence exists; no canonical assets published | define/reproduce release artifacts |
| Integrity/signing policy | SHA-256 evidence exists; signing policy not selected | choose signing/verification authority |
| Public installer switch | intentionally unchanged | switch only after review |
| Service/upgrade rollback | local backup/deploy behavior qualified | define release-to-release rollback procedure |
| Release docs/README | current Python authority is accurate | make cutover edits in M11 only |
| Canonical Rust updater | O008 implementation exists but is not public authority | publish and authorize release metadata |
| Config/DB/runtime paths | preserved and qualified | retain paths during cutover |
| Initial fallback/rollback | not exercised as a public release | document and rehearse M11 procedure |

### CI recommendation

Keep the normal CI gate lean: Ruff, Pyright, Python smoke, and the existing
Rust/migration focused tests that are already invoked by source changes. Do not
add live-provider, rootful, physical-SBC, screenshot, Q009 stability, or the
full serial Rust suite to every CI run. Before any M11 cutover review, add a
manual qualification workflow that runs the current Q001 validator and Q002
aggregate plus the Q003/Q004 runners; retain Q005-Q009 as manual hosted,
disposable, live, or physical qualification as appropriate.

## Dependency, schema, security, and documentation review

Q010 adds no Rust/runtime dependency, database migration, production task, or
public release behavior. Current artifacts contain no credentials, raw provider
bodies, hostnames, addresses, serial numbers, or full environment dumps. The
live credential name is recorded only as an allowlisted variable name.

README, `docs/`, and `architecture/` were searched for platform, installer,
update, systemd/cron, provider/transcoding, backup/recovery, dashboard, and
SBC claims. They remain consistent with the qualified reality: Python remains
the public authority, Rust is a qualification candidate, and M11 cutover has
not occurred. No M11 documentation edit was pulled into Q010.

## Registry and roadmap transition

Q010 is closed as a blocked attempt, not accepted. Q011 is recorded as the
narrow corrective plan for Q010-F001 and requires maintainer-authorized live
provider access. Q008, Q009, and M11 remain blocked; no future plan is
dependency-ready. Once Q011 is accepted, the only valid promotion order is
Q008, then Q009, then a new Q010 acceptance review; only that accepted Q010
may make M11 eligible for its separate planning review.

//! Shared, pure semantic adaptation and loss policy.
//!
//! Compatibility facade: the implementation lives in the `eggpool-wire`
//! workspace crate. EggPool-owned wrappers stay in `wire::adapters`.

pub use eggpool_wire::adaptation::*;
